由于在大规模网络数据集上进行预训练的视觉或语言模型的兴起，出现了大量以开发通用机器人为目标的工作，目前主要有两种有效的方法：(i)直接利用预训练模型，将语言落到空间表征，进行“感知-规划-执行”等相关操作，利用预训练模型对世界的认知完成各类通用任务，以及(ii)使用预训练模型为基础模型，结合机器人示教数据，使用扩散模型或对预训练模型进行有监督学习生成输出连续的动作。第一类方法通过任务与场景理解进行长轨迹规划，泛化性与可解释性强，但是受限于预训练模型推理速度较慢，执行过程中实时性较差，难以应对突发状况，第二类方法具备高频率输出，执行效率高，但是缺乏全局约束，且可解释性较差。那么提出问题，我们到底应该如何平衡高层语义规划的泛化性与底层动作执行的实时性。

我们注意到，越来越多研究领域开始引入快慢双系统架构：慢系统负责高层语义规划与长轨迹生成，强调对任务意图、环境语义和长期目标的理解与泛化能力，通常运行在较低频率但具备更强的推理与抽象能力；快系统则专注于连续、高频的实时控制信号输出，强调低延迟、高带宽的执行效率，以确保系统在动态环境中维持稳定响应速度。利用快慢双系统架构的方式可以去平衡上述所提到的问题，那么在通用机器人操作任务中，一个根本性问题随之浮现：如何在不牺牲全局语义引导的前提下，克服慢系统依赖的大型视觉语言模型（LVLM）推理延迟高、无法实时更新的固有缺陷，并使快系统能够在环境动态变化时快速响应局部扰动，同时始终不偏离慢系统所规划的全局轨迹意图？

应对这一挑战时，我们发现基于视觉提示的模型现在有了很大进步

