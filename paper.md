Introduction：

由于在大规模网络数据集上进行预训练的视觉或语言模型的兴起，出现了大量以开发通用机器人为目标的工作，目前主要有两种有效的方法：(i)直接利用大型预训练模型，将语言落到空间表征，进行“感知-规划-执行”等相关操作，利用预训练模型对世界的认知完成各类通用任务，以及(ii)使用预训练模型为基础模型，结合机器人示教数据，使用扩散模型或对预训练模型进行有监督学习生成输出连续的动作。第一类方法通过任务与场景理解进行长轨迹规划，泛化性与可解释性强，但是受限于预训练模型推理速度较慢，执行过程中实时性较差，难以应对突发状况，第二类方法具备高频率输出，执行效率高，但是缺乏全局约束，且可解释性较差。因此我们提出问题，到底应该如何平衡高层语义规划的泛化性与底层动作执行的实时性。

我们注意到，越来越多研究领域开始引入快慢双系统架构：慢系统负责高层语义规划与长轨迹生成，强调对任务意图、环境语义和长期目标的理解与泛化能力，通常运行在较低频率但具备更强的推理与抽象能力；快系统则专注于连续、高频的实时控制信号输出，强调低延迟、高带宽的执行效率，以确保系统在动态环境中维持稳定响应速度。利用快慢双系统架构的方式可以去平衡上述所提到的问题，那么在通用机器人操作任务中，一个根本性问题随之浮现：如何在不牺牲全局语义引导的前提下，克服慢系统依赖的大型视觉语言模型（LVLM）推理延迟高、无法实时更新的固有缺陷，并使快系统能够在环境动态变化时快速响应局部扰动，同时始终不偏离慢系统所规划的全局轨迹意图？

在当前工作，我们提出了一种基于“快慢双系统”（Fast-Slow Dual System）的通用机器人操作框架。具体来说，该框架将任务执行解耦为两个协同模块：慢系统负责利用大型视觉语言模型（LVLM）和三维价值图（如 Voxposer）进行高层语义理解与长时轨迹规划，生成语义合理、全局一致的稀疏轨迹；快系统则基于实时感知（YOLOE）和慢系统提供的轨迹参考点，通过候选点采样与动态权重分配机制，高频生成局部动作流。快系统在保持对全局意图遵循的同时，能够即时响应环境动态变化（如障碍物移动或目标偏移），实现闭环、鲁棒的实时控制。两个系统通过共享世界状态与轨迹引导信号紧密耦合，形成“长轨迹引导 + 动作流执行”的协同范式。

通过在多种开放世界操作任务上的实验，我们验证了所提框架的有效性：系统仅需自然语言指令与 RGB-D 输入，无需任务特定训练或环境先验模型，即可完成多阶段、对突发扰动具有反应能力的复杂操作（如“抓取杯柄并避开旁边的花瓶”）。得益于 YOLOE 的高频感知与快系统的轻量决策机制，整体动作流生成频率可达 10 Hz 以上；同时，慢系统周期性更新的全局轨迹确保了长时任务的语义一致性与目标可达性。实验表明，该架构在保持端到端方法实时性的同时，显著提升了分层方法在动态环境中的适应能力。

本文的主要贡献包括：1）提出“快慢双系统”架构，将 Voxposer 的长轨迹规划与实时感知有机结合生成实时的动作流，为通用机器人操作应对动态扰动提出了一种新颖的思路；2）设计了一套模块化技术路线，融合 Qwen2.5-VL 的语义解析、YOLOE 的开放世界高频检测与 Voxposer 的三维损失图规划，构建了从语言指令到实时动作的完整闭环；3）在真实机器人平台上实现了无需微调、支持自由语言指令的通用操作系统，展示了其在多阶段、非结构化场景下的泛化性与鲁棒性。





method：

在这部分我们首先介绍A为问题表述，然后我们介绍了框架中的两个关键部分B实时感知与C快慢系统。

A.Problem Formulation

大多数通用机器人操作分为三个部分，感知、规划、执行，首先给定一个自然语言$L$(e.g.,"Press the green button")，使用VLM分解生成一系列高水平行动规划$(l_1,l_2,...,ln)$，同时在该时间步$t_i$下由RGB-D相机得到的场景观察为$o_i$，



实验：

| Tasks                       | ours |
| --------------------------- | ---- |
| Insert flower into vase     |      |
| Pour tea                    |      |
| Recycle all object in scene |      |
| Open the drawer             |      |
| Press the green button      |      |
| Open the bottle cap         |      |
|                             |      |

| Tasks                                  | ours |
| -------------------------------------- | ---- |
| Moving Object                          |      |
| Static Single Obstacle+Moving Object   |      |
| Static Multiple Obstacle+Moving Object |      |
| Moving Single Obstacle+Moving Object   |      |
| Moving Multiple Obstacle+Moving Object |      |
|                                        |      |

